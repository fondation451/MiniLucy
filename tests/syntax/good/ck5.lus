node ck5 (b,c: bool) returns (e: bool);
var c_prime: bool;
let
  c_prime = True -> pre c;
	e = current (edge(b when True(c_prime)));
tel

node edge(x:bool) returns (y: bool);
let
  y = False -> x and not pre(x);
tel

